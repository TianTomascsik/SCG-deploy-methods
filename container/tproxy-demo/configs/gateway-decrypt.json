{
  "log_level": "info",
  "sock_buf_size": 16777216,
  "rules": [
    {
      "name": "decrypt-inbound",
      "direction": "decrypt",
      "listen_addr": "0.0.0.0:4000",
      "listen_proto": "tcp",
      "upstream_addr": "auto",
      "upstream_proto": "tcp",
      "transparent": true,
      "security_provider": "tls",
      "protocol_version": "tls1.3",
      "intercept": {
        "mode": "tproxy",
        "match_dports": "8080",
        "match_src": ["172.30.0.0/24"]
      }
    }
  ]
}
